# Frontend — Next.js

Vazio até o Épico 1.

Antes de escrever JSX: leia `src/components` e use o que existe.
Cor vem de token em `src/app/globals.css`, nunca literal.
Regras completas em [`../docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md) §7.
